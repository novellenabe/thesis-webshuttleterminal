//https://shuttle-monitoring-terminal.lcl.host:44388
import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
